services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gmail_app

  gtest:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: gmail_tests
